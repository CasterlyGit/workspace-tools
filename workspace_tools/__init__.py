"""workspace_tools — deterministic SDD pipeline orchestration.

Public API surface: import from `workspace_tools.core` for the building blocks,
`workspace_tools.stages` for pre-built stages, `workspace_tools.pipelines` for
common shapes. Run `python -m workspace_tools.cli` for the CLI."""
__version__ = "0.1.0"
